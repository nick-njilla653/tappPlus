# ===========================================
# TAPP+ - Variables d'environnement
# ===========================================
# Copier ce fichier vers .env et configurer les valeurs

# ===========================================
# ENVIRONNEMENT
# ===========================================
NODE_ENV=production
TZ=Africa/Douala

# ===========================================
# BASE DE DONNÉES POSTGRESQL
# ===========================================
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://meditache:CHANGE_ME_STRONG_PASSWORD@postgres:5432/meditache

# Credentials séparés (pour docker-compose)
POSTGRES_DB=meditache
POSTGRES_USER=meditache
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ===========================================
# REDIS (Cache et Queue)
# ===========================================
REDIS_URL=redis://:CHANGE_ME_REDIS_PASSWORD@redis:6379
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD

# ===========================================
# JWT AUTHENTICATION
# ===========================================
# Générer avec: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_BASE64_32
JWT_REFRESH_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_BASE64_32
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ===========================================
# API BACKEND
# ===========================================
API_PORT=5550

# CORS: Domaines autorisés (séparés par des virgules)
# Exemple: https://votre-domaine.com,https://www.votre-domaine.com
CORS_ORIGINS=https://votre-domaine.com

# ===========================================
# FRONTEND WEB
# ===========================================
# URL publique de l'API (accessible depuis le navigateur des utilisateurs)
# IMPORTANT: Doit être l'URL publique, pas un nom de service Docker interne
NEXT_PUBLIC_API_URL=https://api.votre-domaine.com

# ===========================================
# NOTIFICATIONS - TWILIO (SMS)
# ===========================================
# Optionnel: Laisser vide pour désactiver les SMS
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# ===========================================
# NOTIFICATIONS - EMAIL
# ===========================================
# Optionnel: Laisser vide pour désactiver les emails

# Option 1: Gmail
# EMAIL_USER=votre-email@gmail.com
# EMAIL_PASS=votre-app-password

# Option 2: SMTP personnalisé
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=noreply@votre-domaine.com

# ===========================================
# NOTIFICATIONS - FIREBASE (Push)
# ===========================================
# Optionnel: Laisser vide pour désactiver les notifications push
# Contenu du fichier serviceAccount.json de Firebase
FCM_SERVICE_ACCOUNT=

# ===========================================
# DOMAINES (pour configuration)
# ===========================================
DOMAIN=votre-domaine.com
API_DOMAIN=api.votre-domaine.com

# ===========================================
# BACKUP
# ===========================================
BACKUP_RETENTION_DAYS=7
